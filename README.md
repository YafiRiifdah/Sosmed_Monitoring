# Sosmed Monitoring Frontend

React dashboard for Instagram engagement monitoring.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

Default dashboard URL:

```text
http://localhost:3000
```

Set `VITE_API_BASE_URL` in `.env` to point to the backend API.

## Docker

```bash
docker compose up --build
```
